spring:
  application:
    name: backend

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
#    properties:
#      hibernate:
#        format_sql: false

  liquibase:
    enabled: false

#  datasource:
#    hikari:
#      maximum-pool-size: 5
#      minimum-idle: 2
#      connection-timeout: 20000
#      idle-timeout: 300000
#      max-lifetime: 1200000

jwt:
  secret: test-jwt-secret-key-for-testing-purposes-only-this-must-be-at-least-256-bits-long

admin:
  secret: super_admin_token_test

app:
  upload:
    photo-dir: target/test-uploads/photos

logging:
  level:
    com.backend: INFO
    org.springframework.web: WARN
#    org.hibernate: WARN
#    org.testcontainers: INFO
#    com.zaxxer.hikari: WARN